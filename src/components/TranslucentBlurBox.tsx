import styled from '@emotion/styled'
import { Box } from '@mui/material'

export const TranslucentBlurBox = styled(Box)`
  background-color: rgba(43, 43, 44, 0.4);
  backdrop-filter: blur(30px);
  border-radius: 16px;
`
